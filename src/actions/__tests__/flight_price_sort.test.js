var React = require('react');
var FlightPriceSort = require('../flight_price_sort.js')

test('', () => {
	var data = {
		minValue : 0,
		maxValue : 100,
		searchResult : {
			result : {
				departure : []
			}
		}
	}
});